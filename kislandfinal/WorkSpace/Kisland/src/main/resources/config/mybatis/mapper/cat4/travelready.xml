<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ready">
	<!-- 파일 마다 있는 SQL문들을 구분할수 있는 이름 -->
	<!-- 사용할 SQL문들을 정의하는 파일 -->

	<select id="selectReady" resultType="all.vo.InfoBoardVO">
		select * from infoboard where no between 1 and 6
	</select>
</mapper>